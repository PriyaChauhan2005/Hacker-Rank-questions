#include<bits/stdc++.h>
using namespace std;
int main(){
    long n,i,min,max;
    n=5;
    long a[n];
    for(i=0;i<n;i++){
        cin>>a[i];
    }
    sort(a,a+5);
    min=a[0]+a[1]+a[2]+a[3];
    max=a[1]+a[2]+a[3]+a[4];
    cout<<min<<" "<<max;
}
