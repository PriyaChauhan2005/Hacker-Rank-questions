#include<bits/stdc++.h>
using namespace std;
int main(){
    int i,j,tall,n,number=0;
    cin>>n;
    int a[n];
    for(i=0;i<n;i++){
        cin>>a[i];
    }
    sort(a,a+n);
    tall=a[n-1];
    for(i=0;i<n;i++){
        if(tall==a[i]){
            number++;
        }
    }
    cout<<number;
}
